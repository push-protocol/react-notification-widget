query PartnerInfo($input: PartnerInfoInput!) {
  partnerInfo(input: $input) {
    id
    channelAddress
    logo
    name
    chainId
    slug
    discordGuildUrl
    messageCategories {
      id
      name
      description
    }
  }
}
