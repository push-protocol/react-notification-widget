query UserCommunicationChannels ($address: String!) {
    userCommunicationChannels(address: $address) {
        email {
            exists
            hint
        }
        telegram {
            exists
            hint
        }
        discord {
            exists
            hint
        }
    }
}
